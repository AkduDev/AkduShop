# Base de datos PostgreSQL (Neon)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# JWT Secret (para autenticación) - Generar con: openssl rand -base64 32
JWT_SECRET="tu-secret-aqui"
